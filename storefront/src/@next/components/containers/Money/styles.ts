import { styled } from "@styles";

export const Wrapper = styled.span`
  display: inline-block;
`;

export const Money = styled.span`
  color: inherit;
  display: block !important;
  font-size: inherit;
  font-weight: inherit;
  line-height: inherit;
`;

export const ConvertedMoney = styled.span`
  color: inherit;
  display: block !important;
  font-size: 13px;
  font-weight: inherit;
  line-height: 21px;
`;
