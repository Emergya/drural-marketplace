import { makeStyles } from "@drural/macaw-ui";

export const useStyles = makeStyles(
  () => ({
    textAlignCenter: {
      textAlign: "center"
    }
  }),
  { name: "ProductPurchaseEmailUpdate" }
);
