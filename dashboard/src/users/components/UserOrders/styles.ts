import { makeStyles } from "@drural/macaw-ui";

export const useStyles = makeStyles(
  {
    link: {
      cursor: "pointer"
    },
    textRight: {
      textAlign: "right"
    }
  },
  { name: "UserOrders" }
);
